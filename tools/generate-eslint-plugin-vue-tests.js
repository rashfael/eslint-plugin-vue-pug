const fs = require('fs')
const { execSync } = require('child_process')
const path = require('path')
const { convert: html2Pug } = require('xhtml2pug')
const { parse: parseVue } = require('@vue/compiler-dom')
const SOURCE_BASE_PATH = path.resolve(
  __dirname,
  '../eslint-plugin-vue/tests/lib/rules/'
)
const TARGET_BASE_PATH = path.resolve(
  __dirname,
  '../tests/lib/rules/eslint-plugin-vue/'
)

if (!fs.existsSync(TARGET_BASE_PATH))
  fs.mkdirSync(TARGET_BASE_PATH, { recursive: true })

function getSubmoduleRef() {
  // Try the .git/modules pointer first (works for branch-tracked submodules)
  const candidates = [
    '../.git/modules/eslint-plugin-vue/refs/heads/master',
    '../.git/modules/eslint-plugin-vue/HEAD'
  ]
  for (const rel of candidates) {
    try {
      const content = fs.readFileSync(path.resolve(__dirname, rel), 'utf8').trim()
      if (/^[0-9a-f]{40}$/.test(content)) return content
    } catch {}
  }
  // Fall back to git submodule status
  try {
    const out = execSync('git submodule status eslint-plugin-vue', {
      cwd: path.resolve(__dirname, '..'),
      encoding: 'utf8'
    })
    const match = /[+\- ]([0-9a-f]{40})/.exec(out)
    if (match) return match[1]
  } catch {}
  return 'HEAD'
}

const ESLINT_PLUGIN_VUE_GIT_REF = getSubmoduleRef()

function getHeader(testname) {
  return `// AUTOGENERATED FROM https://github.com/vuejs/eslint-plugin-vue/blob/${ESLINT_PLUGIN_VUE_GIT_REF}/tests/lib/rules/${testname}.test.ts\n`
}

const IGNORE_TESTS = [
  'array-bracket-newline',
  'comment-directive', // invalid syntax breaks the autoconverter
  'html-closing-bracket-spacing',
  'html-closing-bracket-newline',
  'html-comment-content-newline',
  'html-comment-content-spacing',
  'html-comment-indent',
  'html-end-tags',
  'html-indent',
  'html-self-closing',
  'multiline-html-element-content-newline',
  'no-irregular-whitespace', // will probably die horribly
  'no-parsing-error', // need to do own pug parser tests
  'component-name-in-template-casing', // fixes break pug
  'max-attributes-per-line', // fixes break pug
  'max-len', // fixes break pug
  'no-child-content', // does not clean up whitespace, but this might be because we're not using HTMLWhitespace
  'no-multi-spaces', // Error type should be HTMLIdentifier, found PugIdentifier
  'this-in-template', // upstream uses multi-line `${suggestionPrefix(...)}` substitutions in `output:` that don't survive html→pug conversion

  // Bucket E sub-bucket E2 — rule mechanic doesn't fire on pug at all (passInvalid==0).
  // Either the rule walks HTML-only token shapes (HTMLWhitespace, VText, mustache `{{ }}`),
  // or it reaches into JS-AST inside attribute values that the pug tokenizer doesn't expose
  // the same way. See docs/test-triage.md "Bucket E" for the classification methodology.
  'singleline-html-element-content-newline', // filters HTMLWhitespace tokens explicitly; pug emits none
  'mustache-interpolation-spacing', // governs `{{ x }}` whitespace; pug template text uses different syntax
  'no-useless-mustaches', // visits `VElement > VExpressionContainer`; not produced for pug text bodies
  'no-bare-strings-in-template', // walks VText nodes; pug text tokens aren't VText
  'no-duplicate-class-names', // pug class shorthand `.foo.foo` bypasses the `class="..."` selector; the `:class` selector also never fires
  'no-multiple-objects-in-class', // walks ArrayExpression inside `:class`; the JS subtree isn't reached for pug attributes
  'padding-line-between-tags', // enforces blank lines between sibling HTML tags; pug uses indentation-based layout — rule fires on every nested element (false positives on valid pug) and never sees its expected separator pattern on invalid pug. No clean pug analog.

  // tests without template content rules
  'block-lang',
  'no-restricted-call-after-await',
  'script-indent',
  'component-api-style',
  'component-definition-name-casing',
  'component-options-name-casing',
  'component-tags-order',
  'experimental-script-setup-vars',
  'jsx-uses-vars',
  'multi-word-component-names',
  'name-property-casing',
  'new-line-between-multi-line-property',
  'next-tick-style',
  'no-arrow-functions-in-watch',
  'no-async-in-computed-properties',
  'no-boolean-default',
  'no-computed-properties-in-data',
  'no-deprecated-data-object-declaration',
  'no-deprecated-destroyed-lifecycle',
  'no-deprecated-events-api',
  'no-deprecated-vue-config-keycodes',
  'no-dupe-keys',
  'no-empty-component-block',
  'no-export-in-script-setup',
  'no-expose-after-await',
  'no-invalid-model-keys',
  'no-lifecycle-after-await',
  'no-multiple-slot-args',
  'no-potential-component-option-typo',
  'no-ref-as-operand',
  'no-reserved-props',
  'no-restricted-block',
  'no-restricted-component-options',
  'no-restricted-props',
  'no-setup-props-destructure',
  'no-shared-component-data',
  'no-side-effects-in-computed-properties',
  'one-component-per-file',
  'prefer-import-from-vue',
  'prop-name-casing',
  'require-direct-export',
  'require-expose',
  'require-name-property',
  'require-prop-type-constructor',
  'require-render-return',
  'require-slots-as-functions',
  'return-in-computed-property',
  'return-in-emits-validator',
  'valid-define-emits',
  'valid-define-props',
  'valid-next-tick'
]

let ruleTests = fs
  .readdirSync(SOURCE_BASE_PATH)
  .filter((file) => file.endsWith('.test.ts'))
  .map((file) => file.replace(/\.test\.ts$/, ''))
  .filter((file) => !IGNORE_TESTS.includes(file))

function stripIndent(code) {
  const match = code.match(/^[ \t]*(?=\S)/gm)
  if (match[0] === '') match.shift() // ignore first line if not indented
  const indent =
    match && match.length
      ? match.reduce((i, a) => Math.min(i, a.length), Infinity)
      : 0
  if (indent === 0) return code
  return code.replace(new RegExp(`^[ \\t]{${indent}}`, 'gm'), '')
}

function convertCode(code) {
  try {
    const ast = parseVue(code)
    for (const node of ast.children) {
      if (node.tag !== 'template' || node.children.length === 0) continue
      const templateContent = code.slice(
        node.children[0].loc.start.offset,
        node.children[node.children.length - 1].loc.end.offset
      )
      let convertedTemplate = html2Pug(stripIndent(templateContent), {
        bodyLess: true,
        parser: 'vue'
      })
      // fix borked empty attributes
      convertedTemplate = convertedTemplate
        .replaceAll('=,', '="",')
        // escape backticks so the wrapper template literal isn't terminated
        .replaceAll(/([^\\])`/g, '$1\\`')
        .replaceAll(/([^\\])`/g, '$1\\`') // MULTIPASS
        .trim()
      const isMultiline = convertedTemplate.includes('\n')
      code = `${code.slice(0, node.loc.start.offset)}<template lang="pug"${code
        .slice(node.loc.start.offset + 9, node.children[0].loc.start.offset)
        .trimEnd()}${isMultiline ? '\n' : ''}${convertedTemplate}${
        isMultiline ? '\n' : ''
      }${code
        .slice(node.children[node.children.length - 1].loc.end.offset)
        .trim()}`
    }
    return code
  } catch (_error) {
    return `<!-- CONVERT ERROR -->${_error.message.replaceAll(
      /([^\\])`/g,
      '$1\\`'
    )}${code}`
  }
}

if (process.argv[2]) ruleTests = [process.argv[2]]

for (const testname of ruleTests) {
  let test = fs.readFileSync(
    path.join(SOURCE_BASE_PATH, `${testname}.test.ts`),
    'utf8'
  )
  const target = path.join(TARGET_BASE_PATH, `${testname}.test.ts`)
  try {
    const targetContent = fs.readFileSync(target, 'utf8')
    if (targetContent.startsWith('// SKIP AUTOGENERATION')) {
      console.log('Skipping rule:', testname)
      continue
    }
  } catch (_error) {
    /* file does not exist yet */
  }
  console.log('Generating test for rule:', testname)

  // Rewrite ESM imports from upstream paths to our test layout.
  test = test
    // any rule import from '../../../lib/rules/<name>' -> dist (covers cross-rule imports too)
    .replace(
      /from '\.\.\/\.\.\/\.\.\/lib\/rules\/([^']+)'/g,
      `from 'eslint-plugin-vue/dist/rules/$1'`
    )
    // import { ... } from '../../eslint-compat' -> our local copy
    .replace(/from '\.\.\/\.\.\/eslint-compat'/g, `from '../../../eslint-compat'`)
    // import { ... } from '../../test-utils/<x>' -> reach upstream test-utils
    .replace(
      /from '\.\.\/\.\.\/test-utils\/([^']+)'/g,
      `from '../../../../eslint-plugin-vue/tests/test-utils/$1'`
    )
    // upstream sometimes pulls from utils via relative import — map onto dist
    .replace(
      /from '\.\.\/\.\.\/\.\.\/lib\/utils\/([^']+)'/g,
      `from 'eslint-plugin-vue/dist/utils/$1'`
    )
    // upstream's bundled plugin (`pluginVue from '../../../dist'`) -> the package itself
    .replace(/from '\.\.\/\.\.\/\.\.\/dist'/g, `from 'eslint-plugin-vue'`)
    // upstream's processor module (`from '../../../lib/processor'`) -> dist
    .replace(
      /from '\.\.\/\.\.\/\.\.\/lib\/processor'/g,
      `from 'eslint-plugin-vue/dist/processor'`
    )
    // Inject the pug template tokenizer into both shapes upstream uses.
    // (1) Existing parserOptions: {...} — splice templateTokenizer into it.
    .replaceAll(
      /parserOptions: \{(.*?)\}/gs,
      "parserOptions: {$1, templateTokenizer: { pug: 'vue-eslint-parser-template-tokenizer-pug'}}"
    )
    // (2) languageOptions blocks WITHOUT a parserOptions key — add one.
    //     Without templateTokenizer, vue-eslint-parser sees `<template lang="pug">…</template>`
    //     as raw text and never invokes the pug tokenizer at all, so any
    //     rule that walks the template AST silently sees nothing.
    //     Match `languageOptions: {…}` (or `const languageOptions = {…}`) and
    //     check the inner body for `parserOptions:`; if absent, append.
    .replace(
      /((?:languageOptions\s*[:=]|const\s+languageOptions\s*=)\s*)\{([^{}]*(?:\{[^{}]*\}[^{}]*)*)\}/g,
      (match, lhs, body) => {
        if (/parserOptions\s*:/.test(body)) return match
        const sep = body.trim() ? ',\n    ' : '\n    '
        return `${lhs}{${body.replace(/\s+$/, '')}${sep}parserOptions: { templateTokenizer: { pug: 'vue-eslint-parser-template-tokenizer-pug' } }\n  }`
      }
    )
    // Convert <template>...</template> snippets to pug equivalents.
    // try to get simple test cases
    .replaceAll(
      /^(?!.*[^:]\/\/)(.*?)(['"`])(\s*?<template>[^'"`](.|\n)*?[^\\])\2/gm,
      (_match, lineStart, _quotes, code) => `${lineStart}\`${convertCode(code)}\``
    )
    // try to catch stragglers with other tags before template
    .replaceAll(/^ *<template>.*?<\/template>/gms, (match) => convertCode(match))

  fs.writeFileSync(target, getHeader(testname) + test)
}
