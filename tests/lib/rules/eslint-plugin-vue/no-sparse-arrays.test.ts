// AUTOGENERATED FROM https://github.com/vuejs/eslint-plugin-vue/blob/3868a2d6e7b44037dd5aa04a7acdd857a0358a7b/tests/lib/rules/no-sparse-arrays.test.ts
/**
 * @author Yosuke Ota
 */
import { RuleTester, ESLint } from '../../../eslint-compat'
import semver from 'semver'
import rule from 'eslint-plugin-vue/dist/rules/no-sparse-arrays'
import vueEslintParser from 'vue-eslint-parser'

const tester = new RuleTester({
  languageOptions: {
    parser: vueEslintParser,
    ecmaVersion: 2015,
    parserOptions: {
      templateTokenizer: { pug: 'vue-eslint-parser-template-tokenizer-pug' }
    }
  }
})

tester.run('no-sparse-arrays', rule, {
  valid: [
    `<template lang="pug">div(:class="['foo', 'bar']")</template>`,
    `<!-- CONVERT ERROR -->Attribute name cannot contain U+0022 ("), U+0027 ('), and U+003C (<).<template>
      <div v-bind:[['foo'][0]]="bar" />
    </template>`
  ],
  invalid: [
    {
      code: `
      <template lang="pug">div(:class="[, 'foo', 'bar']")</template>`,
      errors: [
        {
          message: 'Unexpected comma in middle of array.',
          line: 2,
          endLine: 2,
          ...(semver.gte(ESLint.version, '9.5.0')
            ? { column: 41, endColumn: 42 }
            : { column: 40, endColumn: 56 })
        }
      ]
    },
    {
      code: `<!-- CONVERT ERROR -->Attribute name cannot contain U+0022 ("), U+0027 ('), and U+003C (<).
<!-- CONVERT ERROR -->Attribute name cannot contain U+0022 ("), U+0027 ('), and U+003C (<).      <template>
        <div v-bind:[[,'foo'][1]]="bar" />
      </template>`,
      errors: [
        {
          message: 'Unexpected comma in middle of array.',
          line: 3,
          endLine: 3,
          ...(semver.gte(ESLint.version, '9.5.0')
            ? { column: 23, endColumn: 24 }
            : { column: 22, endColumn: 30 })
        }
      ]
    }
  ]
})
