// AUTOGENERATED FROM https://github.com/vuejs/eslint-plugin-vue/blob/3868a2d6e7b44037dd5aa04a7acdd857a0358a7b/tests/lib/rules/no-unsupported-features.test.ts
/**
 * @author Yosuke Ota
 * See LICENSE file in root directory for full license.
 */
/**
 * See to testcases in `./no-unsupported-features` directory for testcases of each features.
 */
import { RuleTester } from '../../../eslint-compat'
import rule from 'eslint-plugin-vue/dist/rules/no-unsupported-features'
import vueEslintParser from 'vue-eslint-parser'

const tester = new RuleTester({
  languageOptions: { parser: vueEslintParser, ecmaVersion: 2019 }
})

tester.run('no-unsupported-features', rule, {
  valid: [
    {
      code: `
      <template lang="pug">
VList
  template(v-slot:name)
    a(:[href]="'/xxx'")
</template>`,
      options: [{ version: '^2.6.0' }]
    }
  ],
  invalid: [
    {
      code: `
      <template lang="pug">
VList
  template(v-slot:name)
    a(:[href]="'/xxx'")
</template>`,
      output: `
      <template lang="pug">
VList
  template(slot="name")
    a(:[href]="'/xxx'")
</template>`,
      options: [{ version: '^2.5.0' }],
      errors: [
        {
          message: '`v-slot` are not supported until Vue.js "2.6.0".',
          line: 4,
          column: 21,
          endLine: 4,
          endColumn: 32
        },
        {
          message: 'Dynamic arguments are not supported until Vue.js "2.6.0".',
          line: 6,
          column: 16,
          endLine: 6,
          endColumn: 22
        }
      ]
    }
  ]
})
